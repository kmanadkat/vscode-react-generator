const PROMPT_STRINGS = {
    scssImport: "Add scss import",
    cssImport: "Add css import"
}

const PROMPT_TYPES = {
    info: 'info',
    warning: 'warning',
    error: 'error'
}

module.exports = {
    PROMPT_STRINGS,
    PROMPT_TYPES
}